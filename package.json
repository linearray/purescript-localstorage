{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.6",
    "purescript-psa": "^0.3.9",
    "rimraf": "^2.5.0"
  }
}
